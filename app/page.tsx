import { Layout } from "@/components/blocks/layout";
import { CityMap } from "@/components/blocks/map";

export default async function Home() {
  return (
    <main>
      <Layout />
    </main>
  );
}
